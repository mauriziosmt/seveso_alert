services:
  seveso-alert:
    build: .
    container_name: seveso-alert
    restart: always
    # Copy .env.example to .env and fill in your Telegram credentials
    env_file:
      - .env
    volumes:
      - .:/app
